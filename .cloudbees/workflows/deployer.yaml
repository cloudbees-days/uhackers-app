apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: deployer
on:
  workflow_call:
    inputs:
      environment:
        type: string
        required: true
      manifest:
        type: string
        required: true

jobs:
  hackers-api:
    if: ${{ fromJSON(inputs.manifest)['uhackers-api']['hackers-api'].deploy }}
    uses: cloudbees-days/uhackers-api/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['uhackers-api']['hackers-api'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['uhackers-api']['hackers-api'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-auth:
    if: ${{ fromJSON(inputs.manifest)['uhackers-auth']['hackers-auth'].deploy }}
    uses: cloudbees-days/uhackers-auth/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['uhackers-auth']['hackers-auth'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['uhackers-auth']['hackers-auth'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
  hackers-web:
    if: ${{ fromJSON(inputs.manifest)['uhackers-web']['hackers-web'].deploy }}
    uses: cloudbees-days/uhackers-web/.cloudbees/workflows/deploy.yaml
    with:
      artifact-url: ${{ fromJSON(inputs.manifest)['uhackers-web']['hackers-web'].url }}
      artifact-id: ${{ fromJSON(inputs.manifest)['uhackers-web']['hackers-web'].id }}
      environment: ${{ inputs.environment }}
    secrets: inherit
    vars: inherit
